FROM node:current-buster
RUN npm install -g hue2mqtt
#ENTRYPOINT ["hue2mqtt", "-m", "$MQTT_PROTOCOL://$MQTT_USER:$MQTT_PASSWORD@$MQTT_SERVER"]
ENTRYPOINT hue2mqtt -m $MQTT_PROTOCOL://$MQTT_USER:$MQTT_PASSWORD@$MQTT_SERVER -p $POLLING_INTERVAL
